*.png binary

# Keep LF in the working tree on Windows so Node/Vite scripts and
# shell helpers behave the same as on macOS/Linux. Binary / Windows
# script rules follow the catch-all so they win.
* text=auto eol=lf
*.png binary
*.{cmd,bat,ps1} text eol=crlf
