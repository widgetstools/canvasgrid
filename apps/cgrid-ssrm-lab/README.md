# VelocityGrid Feature Lab (SSRM)

Same MarketsGrid Feature Lab shell as `cgrid-vg-lab`, but each tab uses **`MockSSRMDataProvider`** (sparse SSRM v2 + live ticks, no Perspective WASM).

```bash
npm run build:kernel
npm run dev:ssrm-lab   # http://localhost:5197
```
