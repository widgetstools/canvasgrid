# SDD ledger — Cycle 21i Column Groups editor
Plan: docs/superpowers/plans/2026-07-03-cycle-21i-column-groups-editor.md
Branch: cycle21i/column-groups (base: 378da7c)

Task 1: complete (commits 2ff9bba..ed581cc, review clean, 0 findings)
Task 2: complete (commits ed581cc..e1ad8a7, review found 2 Important + 3 Minor, all fixed; verified colId???field derivation matches grid canonical rule propertyChain.ts:1060; 18/18 model tests, full kernel suite green)
  Minor carried to final review: a def that explicitly sets headerName===colId loses the redundant key on project (behaviorally identical, invisible in render)
Task 3: complete (commits e1ad8a7..5c0dc14, review found 2 Important [drop-index no-op; collapse lost on re-render] + minors, all fixed; resolveDrop extracted+unit-tested; panel 6/6, model 23/23, full kernel suite 2669/2669, tsc clean)
  Carried to final review: undefined --cg-row-group-chip-ghost-* tokens resolve to literal fallbacks (PRE-EXISTING idiom, not introduced here); group rows tabIndex=-1 so click-to-select-for-Style-band has no keyboard path (Task 4 owns selection); real-browser drag hit-testing only covered by Task 5 E2E
Task 4: complete (commits 5c0dc14..aa46188, review clean both verdicts, 0 Critical/Important; write-to-paint chain confirmed bg/fg/fontWeight reach group-header painter; panel 11/11, full suite 2674, tsc clean)
  Minor carried to final review: color fields (bg/fg) have no reset-to-default affordance (intentional); needs light/dark visual QA
Task 5: complete (commits aa46188..cafaa04, review clean both verdicts; cloneDefsTree fix for structuredClone-on-functions confirmed correct; 5/6 E2E pass, 1 legit test.fixme = persistence gap; kernel 2674 green)
  Minors carried to final review: cloneDefsTree null-proto passthrough (inert); playwright reuseExistingServer not CI-gated
  DECISION: user chose FIX NOW -> Task 6 added to persist column-group structure overlay
Task 6: complete (commits 44bc7eb..7ead4cb incl fix 6784c71..7ead4cb; review found 1 Important [function-form group headerStyle leaking into snapshot], fixed in toSerializedNodes; re-review RESOLVED single-path, no bypass; model 27, persist 5, full kernel 2682 green, E2E 6/6 incl persistence)
  Minor carried to final: GroupNode.headerStyle typed ColCellOverrides-only while flatten casts away HeaderStyleFunc branch (runtime guard covers it; pre-existing; optional type-precision follow-up)
  User request folded in -> Task 7: expose columnGroupShow (always/open/closed) per column, BOTH inline + Style-band; engine already supports it (editor-only)
Task 7: complete (commit 5ef97b2..ec01558, review clean both verdicts, 0 Critical/Important; model 31, persist 5, panel 14, full kernel 2690 green, E2E 7/7; columnGroupShow both surfaces via shared buildGroupShowControl; persistence free via Task 6 overlay)
  Minor carried to final: explicit-null-in-static-defs round-trip untested (correct by inspection)
FINAL whole-branch review (378da7c..ec01558, opus): READY TO MERGE, 0 new Critical/Important; all carried Minors triaged acceptable. Integration seams (overlay round-trip, restore-vs-columnState, no save loop, Apply-only, tokens-only) all pass. NOTE: runtime user-collapse state NOT persisted (only authored openByDefault) — pre-existing, authoring-scope; surfaced to user as optional follow-up.
VISUAL QA: panel screenshotted light+dark — professional, consistent with settings chrome, ungrouped-omit confirmed, Style band + children-visibility render correctly. PASS.
Task 8: complete (commits be9ab2b..a1dd24e; persist runtime column-group open state; review both verdicts PASS, code correct; Important test-rigor gap CLOSED with discriminating overlay-group+openByDefault-override test a1dd24e; persist 11/11, kernel ~2695 green, E2E 8/8)
  Note: aggIncremental.perf.test.ts is a pre-existing timing flake (unrelated), green in isolation
Task 9: complete (commit a1dd24e..46b8a8b; Style band parity — italic/underline/fontSize/align/all-sides border; review both verdicts PASS, 0 Critical/Important; merge-correctness verified; panel 19/19, persist 12/12, full kernel 2702, E2E 9/9)
  Minor carried: empty border:{all:{}} husk possible on partial border-clear (no functional impact, matches brief semantics); per-side borders deferred (all-sides shipped)
Task 10: complete (commits 0a9f37e..719c109 = plan+impl a3b9d17+trailing-fix 719c109; horizontal expand/collapse caret on regular column-group headers, AFTER caption per user; caret only when group has columnGroupShow/subgroup child (ag-grid parity); open=chevron-left closed=chevron-right; pivot preserved; review both verdicts PASS 0 Crit/Imp; unit 8/8, kernel 2709/2710 [1 unrelated perf flake], E2E 10/10; VISUALLY CONFIRMED via grid-header screenshot — Trade/Valuation carets trailing, Risk none)
  Minors carried: no committed pixel-level test for icon ternary/trailing math (pre-existing painter pattern, visually verified); single-line caption/caret overlap unguarded (pre-existing sort-icon pattern)

# ── SDD ledger — colgroups demo app (separate plan, same branch) ──
Plan: docs/superpowers/plans/2026-07-04-colgroups-demo.md
Base: 04795a2 (colgroups plan commit)

Task 1: complete (commits 04795a2..b315349 incl fix b315349; review found 1 Critical [tsc -b emitting vite.config.js via composite/references] + 2 Important, all fixed by matching apps/showcase standalone-tsconfig pattern; re-review clean both verdicts; typecheck+build green, no stray emit)
Task 2: complete (commit b315349..c48e3c7; synthetic positions data + formatters; TDD 7/7; review clean both verdicts, 0 findings; PositionRow contract verbatim per brief, up100bp/down100bp correctly deferred to Task 3)
Task 10 refinement (da0ac30): caret moved from far-trailing-edge to IMMEDIATELY AFTER the caption (measure caption width + place chevron just past it, clamped to cell edge) per user; kernel 2710/2710, caret unit 8/8, E2E 10/10; VISUALLY CONFIRMED (Trade </Valuation < carets adjacent to captions, Risk none)
Task 3: complete (commit c48e3c7..d21dfd7; all 7 grouping structures + centerpiece grp-risk mixing leaf fields AND nested sub-groups each in always/open/closed; cross-task data.ts edit added up100bp/down100bp preserving determinism; TDD columnDefs 8/8 + data 7/7; typecheck clean; review clean both verdicts, 0 findings; centerpiece states verified line-by-line)
Task 4: complete (commit da0ac30..d155da1 [d155da1 only; da0ac30 is unrelated USER kernel commit on shared branch]; page shell wires darkTheme+columnDefs+data, legend chips, expand/collapse-all over GROUP_IDS via setColumnGroupOpened [confirmed on GridApi gridApi.d.ts:670]; all 3 E2E test hooks present; typecheck+build clean; review clean both verdicts, 0 findings)
NOTE: user is doing concurrent cycle21i kernel commits on this branch (e.g. da0ac30 registry.ts). Scope each review to the colgroups commit only; final review must scope to colgroups files.
Task 11: complete (commits b5cc0aa..6694e40 + edge-fix 9a5eb15; reserve caret space in group-header caption via ellipsizeToWidth so caret never overlaps caption; review both verdicts PASS; Important edge case [0<maxW<measure(ellipsis) returned over-width dots] FIXED + regression test; stale JSDoc fixed; caret 18/18, kernel 2720/2720; VISUAL confirmed Valuat...< no overlap)

NOTE: branch cycle21i/column-groups is SHARED with a concurrent session building apps/colgroups (ag-grid reference app). My feature work = packages/kernel + apps/cgrid-customizer-demo ONLY. Do NOT merge/rebase without coordinating (would sweep in concurrent colgroups work). INCIDENT: rm -rf apps/colgroups deleted untracked probe*.mjs/e2e/playwright.config (unrecoverable); tracked files restored via git restore.
Task 5: complete (commits 6b4c925 [E2E] + 835bc65 [dark-theme fix]; interleaved with unrelated user kernel commits). Playwright E2E gate: initially 6/7 — caught a REAL mandatory-constraint bug: themeQuartz.withParams(params, "dark") gated dark palette to data-ag-theme-mode="dark" (never set) => grid rendered stock LIGHT in headless. Fixed by dropping the mode arg so dark params apply as base ($default) unconditionally. Now 7/7, grid confirmed dark via computed colorScheme. Implementer also corrected 3 AG-grid DOM selectors + widened viewport to 3400px (all justified vs ag-grid source). Review clean both verdicts. NOTE: plan doc Task 5 draft had Coverage columnGroupShow=closed assertion backwards; implementer fixed the real test (region shows when CLOSED) — shipped code/tests correct, only the plan example was stale.
FINAL whole-branch review (719c109..HEAD scoped to apps/colgroups, opus): READY TO MERGE, 0 Critical/Important. All 7 permutations + centerpiece #6 verified. Only Minor polish: legend cited group #numbers not on headers, missing caret note, orphaned tsconfig.node.json (kept for showcase parity).
POLISH (commit e4758b6): fixed legend copy to use group NAMES + caret hint, added inline SVG favicon (killed favicon 404). Typecheck+build clean, E2E 7/7 re-verified, dark theme visually confirmed via screenshots (collapsed + expanded). Dev server + automation browser killed.

# ── SDD ledger — Cycle 21i Phase 2: customizer foundations + intrinsic toolbar ──
Plan: docs/superpowers/plans/2026-07-04-cycle-21i-phase2-foundations.md
Branch: cycle21i/customizer-phase2 (base: 176d4a5, main after #101)
Decisions: Lit in @cgrid/customizer ONLY (kernel vanilla); toolbar = DOM strip host, NOT canvas subgrid (user correction; first attempt discarded, preserved on origin/cycle21i/customization-2)

Task 1: complete pending G1 (commit 8f35f79; ToolbarHost DOM strip at very top of inset chain [toolbar → top status bar → pivot → row group → headers]; intrinsic default-on w/ toolbar:false opt-out + toolbarHeight, both runtime via updateToolbar; date picker + save icon end zone; getToolbar() onSave/onDateChange/getDate/setDate + start-zone builders; typed toolbarSave/toolbarDateChanged events; .cg-toolbar tokens off shared chrome tokens; optionSchema drift guard caught new options → Appearance band entries added)
  Verified: kernel 2758/2758 (incl. 9 new toolbarHost unit), tsc clean, build clean; browser both apps both themes; inset stacking exact (toolbar 0-40, rgPanel 40-72, canvas 72); positions onSave/onDateChange fire
  E2E incident: 2/11 columnGroups specs failed WITH toolbar, passed without (bisected via stash). Root cause NOT kernel: 40px shift pushed panel rows past the 900px-viewport fold; raw-coordinate dragOnto swept a text selection (screenshot: "Group has no columns" + full-panel selection highlight). Fix: viewport 1400x1400 + scrollIntoViewIfNeeded guard in dragOnto (test-side, precedent = colgroups 3400px widening). 11/11 green.
  Finding for batch review: Column Groups panel has no drag auto-scroll (dragging to an off-screen row requires pre-scrolled view) — pre-existing, surfaced by the toolbar shift; candidate polish item.
  NOTE for G1: journey re-verified live in browser (Custom group + cusip lands in defs via real drag with toolbar present).

Task 2: complete (commit 4bccc76; ModuleStateRegistry — registerStateModule({id,version,get,set}) → GridState.modules {version,data} envelopes riding stateUpdated→persistState autosave; graceful per-slice restore [unknown id / throwing set() → warn+skip]; STATE_SCHEMA_VERSION 3→4 with migrator relocating legacy columnGroupDefs/columnGroupOpen into modules.columnGroups — ONE restore path at former 0c/0d position; kernel columnGroups slice is first registered module; typed moduleStateChanged event + bus remap [columnDefsChanged/columnGroupOpened→'modules']; notifyModuleStateChanged public; types exported for engine packages)
  Verified: registry unit 12/12 (incl. v3→v4 compat fixtures, v1 chain), columnGroupsPersist 15/15 updated to envelope shape (v3 fixtures now exercise the REAL migration path), kernel 2770/2770, tsc + build clean, customizer-demo E2E 11/11 (open-state persistence spec's localStorage wait updated to envelope: '"groupId":"trade","open":false')
  Note: resetState() does not clear module slices (engines own their reset semantics) — flag at batch review if a unified reset is wanted.

Task 1 FIX (post-T2, commit above): USER-REPORTED — opening any side-bar panel drifted the canvas 40px up under the DOM overlays (floating filters over data rows, header captions misaligned). Root cause: reserveSideBarSpace duplicated the top-inset math and missed toolbarTopInset. Fix: shared computeTopInset() used by BOTH applyVerticalInsets and reserveSideBarSpace (also fixes pre-existing shared-strip double-count in the side-bar path). New e2e/overlayAlignment.spec.ts pins geometry across panel open/close + runtime toolbarHeight change. Kernel 2770/2770, E2E 13/13, browser-verified with panel open.
  LESSON for batch review: inset math was duplicated across two reflow paths — grep for other setHostBounds callers if new strips land.

Fix (user report 2026-07-04, commit above): removing ALL column groups didn't survive reload on grids authored WITH groups (slice omitted when tree flat → authored groups resurrected → header band re-inflated). columnGroups module get() now persists a flat overlay when constructedWithGroups; flat-since-construction grids still omit. Unit round-trip added (columnGroupsPersist 14/14... 16/16 with new), kernel 2771/2771, browser-verified via panel delete-all → Apply → reload → 44px header.

Feature (user request 2026-07-04, commit above): status bar INTRINSIC — visible by default in every grid (undefined/true → defaultStatusBarDef(): totalAndFiltered left, selected+aggregation right, bottom; false opts out), runtime-settable ('statusBar' in RUNTIME_OPTION_SET → updateStatusBar mount/unmount/setStatusBarDef), 'Status bar' switch in Grid Options Appearance band. INCIDENT during rollout: intrinsic bottom bar covered the side bar's panel footers (sidebar CSS bottom:0) → Apply/Reset unclickable → 8 E2E timeouts; fixed with SideBarHost.setBottomOffset wired through applyVerticalInsets. Kernel 2773/2773, E2E 13/13, browser-verified (bar on by default, switch round-trip, Apply clears strip at 685<693).

Task 3: complete (commit above; Tier B widening getSortModel/getFilterModel/getState/setState onto CGridApi + two-tier contract JSDoc; enumerations listIcons(setName?) [registry fn + CGrid + api], listCellRenderers() [CellRendererRegistry.list, instance truth], forEachColumnGroup(cb) pre-order live-tree walk [ColumnGroupWalkNode exported via types barrel]; setThemeParams/getThemeParams were already api-tier from Phase 1)
  Verified: apiWidening 7/7 (incl. runtime open-flip visibility + flat-grid no-visit), kernel 2780/2780, tsc + build clean, E2E 13/13
  Note: group open-state semantics — groups without openByDefault seed CLOSED (open only gates columnGroupShow children); tests document this.

Task 4: complete (commit above; ModalHost — backdrop + centered dialog on grid root; focus trap w/ document-order normalization [happy-dom returns comma-list query matches in SELECTOR order — focusablesIn() sorts via compareDocumentPosition]; initial focus prefers content over header ✕; ESC/backdrop/✕/api/destroy typed close reasons; prefers-reduced-motion; z-index 40 above strips; getModal() lazy on class + api; PopupHost NOT extended — cell-anchored positioning is a different concern)
  Verified: modalHost 8/8, kernel 2788/2788, E2E 13/13, browser smoke via toolbar badge-info button (open + focus + ESC)
  Finding for batch review: build-lucide drops <circle> elements — 'info'/'circle-alert'/'circle-help' d-strings are missing their rings (used 'badge-info' instead); extractPaths needs circle→path conversion.

Task 5: complete (commit above; @cgrid/customizer Lit bootstrap — lit 3.3 + @lit/context 1.1 in customizer ONLY, source-direct package; chrome subset cgc-band/field/switch/select/number w/ styles verbatim-ported from kernel .cg-settings-* rules on shared --cg-* tokens [rules cloned, custom props pierce shadow DOM]; controlled components emitting cgc-change; litToolPanel adapter + CgcPanelElement + gridApiContext; standard TC39 decorators w/ accessor keyword per repo tsconfig)
  Verified: registration proof 2/2 (mount via kernel registry zero-kernel-change, shadow chrome render, api-tier option round-trip, refresh() reaches element, getGui-before-init throws), customizer tsc clean
  NOTE for G2 (after T6): side-by-side screenshot check vs kernel Grid Options chrome is the visual-parity gate; styles.ts header documents the keep-in-sync contract.

Task 6: complete pending G2 (commit above; #09 Smart Edit panel — first Lit panel end-to-end: engine-owned editSettings state module in wireEditIntoKernel [compact get(), defensive-merge set(), notify-on-update, unregister-on-destroy], smartEditToolPanel(getHandle) with GLOBAL/OPERATIONS/SAFETY bands per doc, immediate-apply + diff rail, op group never empties [engine merge treats empty as revert-to-defaults])
  INCIDENT: TC39 decorators reached the browser untransformed (esbuild passes them through; source-direct package) → Uncaught SyntaxError; chrome components rewritten to classic static-properties Lit with declare fields. LESSON: no decorators anywhere in source-direct packages.
  Verified: edit 164/164, customizer 6/6, kernel 2788/2788 (1 perf-flake rerun green), E2E 13/13; browser G2 evidence: panel visually matches kernel chrome, divide-op toggle → modules.editSettings persisted → survives reload (restore path: persistState setState runs async AFTER sync wireEditIntoKernel → module registered in time; noted as ordering assumption).
  G2 GATE: user hands-on on the Smart Edit tab (localhost:5187) — side-by-side chrome parity vs Grid Options tab.

Task 7: complete (commit above; #10 Bulk Update panel — GLOBAL + DROPDOWN bands per doc; switchRow/numberRow helpers extracted from T6's inline repetition, Smart Edit refactored onto them [tests pinned behavior through the refactor]; demo tab wired)
Task 8 (partial): e2e/editPanels.spec.ts — 3 specs covering both panels end-to-end (shadow-DOM interaction via Playwright piercing, engine write, modules.editSettings autosave, reload restore incl. sideBar-reopen handling). E2E 16/16 first run. Remaining T8: invariance sweep + FINAL batched review + fix wave.

Task 8: COMPLETE — batched closeout review + fix wave (commit 62edb9b).
Review: 8 finder angles (3 correctness, reuse/simplification/efficiency, altitude, conventions) over main...HEAD (11 commits, ~4k lines). 10 CONFIRMED findings fixed in one wave:
  1. ModuleStateRegistry dropped slices restored before their module registered → autosave then erased them permanently. Fixed: orphan buffer (late register replays; snapshot() passes unclaimed slices through).
  2. everHadGroups: delete-all-groups didn't persist for grids whose grouped defs arrive via updateGridOptions. Fixed: sticky flag across snapshots.
  3. Status bar top→bottom runtime flip left stale inline top (overrode bottom:0, bar pinned over headers). Fixed: StatusBarHost.setTopOffset (host-owned, clears on flip) replaces cgrid querySelector positioning.
  4. One Escape closed every grid's modal (document-capture listeners). Fixed: page-level open-modal stack, topmost owns keyboard.
  5. CgcSwitch role=switch used aria-pressed → state invisible to AT. Fixed: aria-checked (+styles).
  6. v4-composed snapshots with legacy top-level columnGroupDefs silently dropped. Fixed: relocation runs regardless of version.
  7. Positions e2e-visual goldens stale (30/31 red vs intrinsic chrome). Fixed: regenerated deliberately, 31/31 green.
  8. numberRow kept invalid typed text while engine enforced old value. Fixed: snap-back.
  9. Per-frame snapshot serialization (edit-settings stringify + column-tree walk per stateUpdated rAF during drags). Fixed: dirty flag + defs-reference cache; toolbar constructor no longer forces layout.
  10. Per-factory custom-element tags grew the permanent registry + pinned destroyed grids' closures. Fixed: one stable tag per panel type; handle getter as instance prop via litToolPanel setup hook.
Deferred minors (logged, not fixed): customizer styles.ts manual clone of kernel settings CSS (drift risk — consider exported css strings/constructable sheet in Phase 3); lucideSvg/todayIso duplication vs renderer iconSvg + date helpers (todayIso is deliberately LOCAL vs editors' UTC — needs a policy decision); 4 setRowGroupPanelTop/setPivotPanelTop call sites hand-compute partial sums (route through applyVerticalInsets); editPanels reload spec conditionally clicks the tab (persisted sideBar state leaks into test concern); normalizeStatusBarOption(true) no longer yields an EMPTY bar (documented semantic change, no in-repo caller); Column Groups panel lacks drag auto-scroll (viewport-height workaround stands); build-lucide drops <circle> elements.
INVARIANCE (final): kernel 2788/2788, edit 164/164, customizer 9/9, customizer-demo E2E 16/16, showcase E2E 151/151, positions functional E2E 257/257 SERIAL (parallel runs flake from worker contention on one vite server — 150 'failures' vanished at --workers=1; run this suite serially), positions visual 31/31 on regenerated goldens.
PHASE 2 COMPLETE: T1-T8 all landed on cycle21i/customizer-phase2 (12 commits). Gates G1 (toolbar) + G2 (Smart Edit panel chrome parity) presented; user proceeded.

DIRECTION CORRECTION (user, 2026-07-04 evening): current implementation "nothing like the starui marketsgrid" (/Users/develop/wfh/starui). Ground truth surveyed: StarUI = SettingsSheet "Grid Customizer" (right overlay drawer ~820px + OS-window popout, 5-category menubar over 17 modules, master-detail w/ numbered Cockpit bands, DIRTY counter, per-card save) + rich PrimaryToolbar (caption/density/filter pills/search/bell/profiles/save-all/date/view menu/kebab→Grid settings) + toggleable Editing/Formatting strips; visual refs at starui/docs/visual-reference/v1. My Phase 2 drift: #09/#10 as sidebar tabs (D-A says only Grid Options is a tab; master-detail panels live in the sheet), raw hand-rolled Lit instead of D-B's Web Awesome + CM6, intrinsic toolbar ≠ PrimaryToolbar.
REVERT (commit above): intrinsic toolbar removed end-to-end; Smart Edit/Bulk Update tabs unwired (+editPanels.spec deleted); positions goldens regenerated (status bar kept — explicit user request). KEPT: customizer panels/chrome/adapter (future sheet modules), module-state registry + orphan buffering, editSettings persistence, ModalHost, API widening, StatusBarHost.setTopOffset (no caller until PrimaryToolbar returns).
NEXT: Phase 3 pivot spec — SettingsSheet shell (drawer + popout + menubar + master-detail + Cockpit atoms + DirtyBus), PrimaryToolbar, D-B stack adoption. Awaiting user direction on spec vs side-by-side demo comparison first.

# ── SDD ledger — Columns Panel Hierarchy + ag-grid-parity drag ──
Plan: docs/superpowers/plans/2026-07-05-columns-panel-hierarchy.md
Spec: docs/superpowers/specs/2026-07-05-columns-panel-hierarchy-design.md
Branch: feature/columns-panel-hierarchy (base/plan-commit: be3d430b87383aa19d92acec131575328abac616)
Review cadence: NO per-task reviewers (standing user directive); SINGLE whole-branch review at T4.

- [ ] T1: group-membership mutation core + moveColumnToGroup/moveColumnGroup
- [ ] T2: hierarchical rendering (carets, tri-state, indent)
- [ ] T3: group-aware drag (reorder + re-parent + indicators)
- [ ] T4: demo + E2E + closeout review + merge
Task T1: complete (commit be3d430..f045f37, 14 tests, suite 2910 green; gate verified). NOTE: getColumnGroupDefs promoted api-closure→CGrid method (documented) — flag at final review.
Task T2: complete (commit f045f37..86f08d0, suite 2912 green; gate verified). Hierarchical render + panel-local collapse + tri-state (reuses computeRowChecked → pivot semantics carry). Group rows have NO drag handle yet (T3 adds group drag). Updated 3 mock-API tests to add getColumnGroupDefs mock (no assertion changes).
Task T3: complete (commit 86f08d0..af16e50, suite 2925 green, +13 drag tests; gate verified). Group-aware drag: resolveDrop+commitDrop → moveColumnToGroup/moveColumnGroup; external routes preserved; drop-line+group-highlight CSS. NOTES for final review: (1) group-row upper/lower-half insertion collapses to one point in T2 flat-DOM model — UX sanity check in T4; (2) reject-highlight reclones tree per mousemove (unprofiled at huge col counts); (3) drop CSS not eyeballed live yet (T4 browser-verify).
Task T4: complete (commits af16e50..b12ffba: bd6dcee E2E + b12ffba fix). Demo needed NO code change (columns panel + nested groups already present). E2E columnsPanelHierarchy.spec.ts 5/5, full demo suite 30/30. CRITICAL bug found+fixed: T1 columnGroupMutation used raw structuredClone → DataCloneError on function-valued colDef fields (valueFormatter etc.) → API/drag broke on any real grid; fixed by sharing cloneDefsTree (extracted to core/columnTree.ts, de-duped columnGroupsPanel copy) + regression tests + flipped 2 test.fail E2E to passing. Kernel 2927 green.
BROWSER-VERIFY (controller, light+dark): PASS. Panel renders full hierarchy matching apps/colgroups — top-level cols (indent 0), Trade group (caret+handle) → Valuation NESTED sub-group (indent 1) → Notional/MktValue (indent 2), P&L/DailyP&L (indent 1), Risk group; tri-state checkboxes; drag handles on every row incl. groups. LIVE DRAG re-parent confirmed: dragged Price (function valueFormatter — the clone-bug case) onto Risk → moved under risk group end-to-end. Dark mode identical hierarchy. Demo state reset; browser + server killed.
FINAL whole-branch review (0006347..b12ffba, opus): READY TO MERGE, 0 Critical/blocking. 1 IMPORTANT (latent, safe-degrading) + 4 Minor. FIX WAVE (commit d738c28): fixed IMPORTANT #1 (anonymous-groupId groups now draggable via shared ensureGroupIds used by panel render + mutation core; regression test) + MINOR reject-dry-run memoize + MINOR already-there no-op assertion. ACCEPTED tradeoffs (unchanged): reorder routes through columnDefs rebuild (fires columnDefsChanged); group-row half-collapse insertion (flat-DOM). Kernel 2929 green; demo E2E 5/5. FEATURE COMPLETE on branch.
- [x] T1 · [x] T2 · [x] T3 · [x] T4 — COLUMNS PANEL HIERARCHY COMPLETE on branch (ready to merge)
MERGED: local squash-merge into main (squash d694fbe on top of spec 0006347), verified (kernel 2929 green, tsc+build clean), pushed d4e0d14..d694fbe to origin/main, branch feature/columns-panel-hierarchy deleted. FEATURE COMPLETE + SHIPPED.

# ── SDD ledger — Grid header column-group drag (move + re-nest) ──
Plan: docs/superpowers/plans/2026-07-06-grid-header-group-drag.md
Spec: docs/superpowers/specs/2026-07-06-grid-header-group-drag-design.md
Branch: feature/grid-header-group-drag (base/plan-commit: 14941731c28145606d3ef9d789d049970c899d7e)
Review cadence: NO per-task reviewers (standing user directive); SINGLE whole-branch review at T3.
- [ ] T1: computeGroupDropTarget + group-drag start/ghost/reorder commit + accessors
- [ ] T2: re-nest indicators + reject affordance
- [ ] T3: demo + E2E + closeout review + merge
Task T1: complete (commit 14941731..8669d51, suite 2938 green; gate verified). computeGroupDropTarget (pure, 7 tests — algorithm fixed for 2 edge cases WITHOUT changing expectations: front-edge + moving-group-into-own-span vs legit reject; reasoning in source comments) + group-drag start/ghost/reorder wiring in columnDrag.ts (leaf drag untouched) + CGridLike accessors (getColGroupPath uses a lazily-built cache invalidated at both columnTree reassignment sites) + 2 real-DOM integration tests (reorder A past B; marryChildren re-nest no-op). moveColumnGroup reused as the only commit path.
Task T2: complete (commit 8669d51..6992ab0, suite 2943 green; gate verified). Re-nest into/out integration tests passed immediately from T1 resolver (kept as regression); +3 reject-affordance tests RED→GREEN (insertion line hidden + no-drop cursor on illegal/married target). NOTES for final review/T3: (1) reject checks only target-group marriage not full isReparent → possible spurious no-drop on intra-married reorder (unreached in fixtures); (2) this.cursor not applied to canvas DOM mid-drag — PRE-EXISTING FeatureChain gap (affects grabbing cursor too); verify live in T3 browser-verify.
Task T3 (demo/E2E): complete (commit 6992ab0..10e88e5). Demo needed NO code change (grouped headers already render). e2e/gridHeaderGroupDrag.spec.ts +3 (2 real canvas-header drags: reorder Trade, re-nest Risk into Trade; +1 API moveColumnGroup) 3/3; full demo suite 33/33. No kernel change; no bug found.
BROWSER-VERIFY (controller): PASS both themes + both ops. Dark: dragged Risk group header → whole group moved as a unit BEFORE Trade (header re-rendered, all Risk cols together). Light: dragged Valuation sub-group header → lifted OUT of Trade to a top-level group (re-nest-out). Ghost/insertion element present mid-drag. Note: this.cursor mid-drag gap (T2 note) is pre-existing + the insertion/ghost IS shown, so drag affordance reads fine. Demo state reset; browser+server killed.
FINAL whole-branch review (39bb7da..10e88e5, opus): NEEDS-WORK → after fix wave: SHIP. 1 CRITICAL + 1 IMPORTANT + minors. FIX WAVE (commit 07906dc, TDD RED→GREEN): #1 CRITICAL nested-group self-drop now no-ops (restored flanking-leaf guards; dead pop-logic removed; corrected 2 mis-specified unit tests to null per spec §4.4.4 — my brief authoring error; +regression for non-last nested group in-span); #2 IMPORTANT colGroupPathCache invalidated at the 3rd (pivot setColumnTree) reassignment site +regression; #3 MINOR reject dry-run also checks source-parent marriage. LEFT: this.cursor not applied mid-drag (pre-existing FeatureChain gap). Kernel 2945 green; demo E2E 3/3. FEATURE COMPLETE on branch.
- [x] T1 · [x] T2 · [x] T3 — GRID HEADER GROUP DRAG COMPLETE on branch (ready to merge)
MERGED: local squash-merge into main (squash 82b92ec on top of spec 39bb7da), verified (kernel 2946 green, tsc+build clean), pushed to origin/main, branch deleted. FEATURE COMPLETE + SHIPPED.

# ── SDD ledger — Perspective look&feel surface #3 (inline data-viz) ──
Plan: docs/superpowers/plans/2026-07-06-perspective-dataviz.md
Branch: feature/look-and-feel (base: bf2baf485e592f9ac43bcb0479091be7f7ce632c)
Foundation (surface #1) already merged on branch (15b2f63). NO per-task reviewers; single review after T2.
- [ ] T1: theme-token sign palette (kernel threading + renderers read p.pos/negColor)
- [ ] T2: curated demo wiring + browser-verify/tune
Task T1: complete (commit bf2baf4..d3a9cf4, kernel 2952 + renderers 287 green; gate verified). Threaded posColor/negColor: cssReader ResolvedTheme → propertyChain (empty→undefined) → CellPaintConfig → @cgrid/renderers colorsFromParams (shared by numeric 6 painters + bars bidirectional/heat). Order: overrides ?? p.pos/neg ?? SEMANTIC. heatCell covered via the same helper.
Task T2: complete (commit d3a9cf4..cacd74f). Demo wires @cgrid/renderers; P&L+Unrealized pnl (sign blue/salmon), Daily P&L bidirectional-bar, Notional heat (static stats domains). Browser-verified light+dark: duotone + bars + heatmap all render theme-driven. +3 E2E; full demo 38/38. MINOR for review: pnl renderer number format lacks thousands separators ($ +1010642.00) — renderer default, cosmetic.
- [x] T1 · [x] T2 — SURFACE #3 data-viz COMPLETE on branch
FLASH REGRESSION FIX (commit 4906269, user-reported): applying pnl/bidirectional-bar/heat renderers dropped the cell-change flash (custom renderers replace the kernel default paint incl. flash; only priceCell painted it). Fixed: exported paintFlashOverlay, called in pnlCell/bidirectionalBarCell/heatCell (matches priceCell). +4 renderers tests (RED→GREEN); renderers 291 green; kernel tsc clean. NOTE for closeout review: broader gap — most @cgrid/renderers skip the flash; a kernel painter wrapper would fix all (scoped here to the 3 in use). Also noted: pnlCell number format ($ +N.00, no thousand separators) is a cosmetic renderer default.

# ── SDD ledger — Perspective look&feel surface #2 (grid-surface polish) ──
Spec: docs/superpowers/specs/2026-07-06-perspective-grid-surface-design.md (commit ac34e7e)
Branch: feature/look-and-feel. Implemented INLINE per user "continue directly to implementation" (skipped plan/SDD ceremony). NO per-task reviewers; whole-branch closeout still pending before merge.
Part A (em-dash nulls, the substantive feature): SUBAGENT-implemented, commit 7c7a1d3, TDD 39 new tests, kernel 2966 green, tsc+build clean, demo tsc clean. Threaded --cg-empty-fg → cssReader ResolvedTheme.emptyFg (fallback totalsFgMuted) → propertyChain target.emptyFg for DATA cells → numberCell/textCell paint emptyCellText glyph in emptyFg when value empty AND valueFormatted empty. New opt-in grid option emptyCellText?:string (default undefined=blank, existing consumers unchanged), threaded per-frame via byRows sharedConfig. Demo opts in with '–'. Report: .superpowers/sdd/surface2-partA-report.md.
BROWSER-VERIFY (controller): PASS both themes. Injected null/empty rows via __cgapi add+filter. Dark + light: text cols (Ticker/CUSIP) show muted '–' (emptyFg #565e6a dark / #9aa3ae light), dimmer than real values ✓. Number cols show 0/0.00 because the format DSL coerces null→'0.00' (valueFormatted non-empty) → em-dash correctly NOT triggered, matches spec §2.3 condition. Status bar Min/Max/Avg of null already show '—' (pre-existing path, consistent).
Part B (header rhythm) + Part C (states): NO CODE CHANGE — Foundation (#1) already delivered them (spec predicted "largely delivered"). Verified live: group headers (Trade/Risk/Valuation) left-aligned = Perspective-correct (centering would need new paint logic = out of scope §6); leaf headers muted mono w/ adequate 40px-header breathing room; cell-focus outline renders Perspective-blue (--cg-range-border-color #6aa9e0); hover (3% whisper) + selection (16% cool-blue) subtle/appropriate. Demo state reset (dark, live on); browser+server killed.
- [x] Part A · [x] Part B/C (verify-only) — SURFACE #2 COMPLETE on branch
CLOSEOUT (whole look-and-feel branch: Foundation 15b2f63 + #3 + flash-fix + #2) + noted follow-ups (flash-gap kernel painter wrapper affecting most @cgrid/renderers; pnlCell number format lacks thousand separators) STILL PENDING before merge.

# ── SDD ledger — Perspective look&feel surface #4 (chrome) + font-size fix ──
Spec: docs/superpowers/specs/2026-07-06-perspective-chrome-design.md (commit 16068fb). Branch: feature/look-and-feel. Implemented INLINE (design brainstormed + approved; user "continue directly"). Scope: intrinsic chrome + demo toolbar. NO per-task reviewers; whole-branch closeout still pending before merge.
Surface #4 (Part A intrinsic chrome + Part B demo toolbar): SUBAGENT-implemented, commit 35c00ab, kernel 2973 green, perspectiveTheme.test 11/11 (+7 new incl. 4 new checkbox tokens), tsc+build clean, demo tsc clean. AUDIT correction: chrome CSS is NOT in host.ts strings — it all lives in tokens.css; host.ts only createElement+className. Found+fixed a specificity gotcha: .cg-row-group-panel/.cg-pivot-panel redeclare --cg-row-group-chip-* on themselves, shadowing a theme-root override → needed a higher-specificity scoped block. New tokens (hairline checkbox + blue tick): --cg-checkbox-panel-border/-checked-bg/-checked-border/-check-color (all fallback-preserving so quartz/starui byte-identical). Wired .cg-columns-panel-pill to read --cg-row-group-chip-* (previously hardcoded color-mix). Demo toolbar: --demo-* re-mapped to Perspective neutrals (dark #16181d/#191c22/#2a2f37/#c3c9d1/#7a828e; light #ffffff/#f6f8fa/#dfe3e8/#1a1f28/#6b7683) + mono font + 4px radii + blue switch accent. Report: .superpowers/sdd/surface4-report.md.
FONT-SIZE FIX (user-reported "all column cells should have same font size"), commit d08abd7, TDD RED→GREEN +2 tests, renderers 293 green: @cgrid/renderers numeric.ts tabularFont appended ` "tnum"` to the canvas font string to request tabular figures, but ctx.font is the CSS font shorthand (no font-feature-settings slot) → invalid space-separated trailing family → canvas SILENTLY IGNORES the assignment and retains the previously-set font → pnl/heat/number cells got an inconsistent size vs text/bar/kernel-default (12px) cells. Fix: tabularFont returns baseFont unchanged (mono theme fonts already render tabular figures); kept as a named seam. NOTE: pre-existing renderers-tsc errors in tickHistory.test.ts + columnStats.test.ts (src-vs-dist CGridApi/ModalHost dual-identity) are UNRELATED (identical with/without my change) — not introduced here.
BROWSER-VERIFY (controller, both themes): PASS. Font: Notional/P&L/Unrealized (old tabularFont cols) now render at the SAME 12px as Mkt Value/DV01/text — uniform across all columns. Chrome: hairline checkboxes w/ blue tick (checked=blue check in hairline box, unchecked=empty hairline box), flat hairline config pills (⠿ Desk × / Row Groups / Values), mono near-black/paper toolbar reads as one piece with the grid. Demo state reset (dark, live on); browser+server killed.
- [x] Surface #4 chrome · [x] font-size fix — COMPLETE on branch
LOOK-AND-FEEL BRANCH now: #1 Foundation 15b2f63 + #3 data-viz (+flash 4906269) + #2 grid-surface 7c7a1d3 + #4 chrome 35c00ab + font fix d08abd7. CLOSEOUT whole-branch review + noted follow-ups (flash-gap kernel painter wrapper; pnlCell thousand-separators) STILL PENDING before merge.
