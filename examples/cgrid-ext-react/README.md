# VelocityGridExt · React (Vite)

Consumes packed tarballs from `dist/tarballs/`, not workspace `*` links.

```bash
# from repo root
npm run examples:install
npm run dev:ext-react
```

Opens [http://localhost:5202](http://localhost:5202).
