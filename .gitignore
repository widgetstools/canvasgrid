# Logs
node_modules
dist
dist-ssr
*.local
.DS_Store

# TS build cache
tsconfig.tsbuildinfo

# Vite cache
.vite/

# Turbo build cache and per-workspace logs (machine-local artifacts)
/.turbo/
**/.turbo/

# npm regenerates per-workspace lockfiles after install; keep only the root one
apps/*/package-lock.json

# Subagent-driven progress ledger (local scratch)
.superpowers/

# Playwright artifacts
test-results/
playwright-report/

# Autonomous task runner logs (one file per task per run)
.runner-logs/

# Local-only: chrome-devtools-mcp scratch screenshots
.playwright-mcp/

# vitest coverage output
packages/*/coverage/
